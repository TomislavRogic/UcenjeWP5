-- create database projekti;

-- use projekti;

-- izrada tablice zaposlenik

--create table Zaposlenik (
--sifra int PRIMARY KEY,
--nadredeni int,
--ime varchar(45),
--prezime varchar(45),
--datumrodenja datetime,
--placa decimal (18,2),
--odjel int
--);

-- izrada tablice odjel

--create table odjel (
--sifra int primary key,
--naziv varchar(45)
--);

-- izrada tablice projekt

--create table projekt(
--sifra int primary key,
--naziv varchar(45),
--datumpocetka datetime,
--datumkraja datetime
--);

-- izrada tablice sudjeluje

--create table sudjeluje(
--zaposlenik int,
--projekt int,
--datumpristupanja datetime
--);



